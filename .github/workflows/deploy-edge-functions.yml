name: Deploy Edge Functions

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Verify Supabase auth
        run: supabase projects list

      - name: Deploy wa-sync
        run: supabase functions deploy wa-sync --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}

      - name: Deploy delete-user
        run: supabase functions deploy delete-user --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
